/* This is where I keep my notes on JavaScript and Google's Services. */

function onOpen() {
    DocumentApp.getUi()
        .createMenu('Jonas Prototype')
        .addItem('Run', 'sandbox')
        .addToUi();
}

function sandbox() {
    
}